name: Deploy Repos A and B

on:
  push:
    branches:
      - '!main'
      - 'feature_version1'

jobs:
  deploycodeA:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code from Repo A
        uses: actions/checkout@v2
        with:
          repository: kalpanauppara/regional_repo
          ref: ${{ github.event.ref }}

      - name: Create a temporary directory
        run: mkdir content_deploy

      - name: Copy test.sh from Repo A to content_deploy
        run: cp mycode/test.sh content_deploy/

  deploycodeB:
    needs: deploycodeA
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code from Repo B
        uses: actions/checkout@v2
        with:
          repository: kalpanauppara/global_repo
          ref: Feature_branch
      
      - name: Copy test.py from Repo B to content_deploy
        run: cp mysourcecode/test.py ../deploycodeA/content_deploy/

  deployToSalesforce:
    needs: [deploycodeA, deploycodeB]
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Salesforce (replace with your deployment command)
        run: |
          # Deploy the merged content from content_deploy to Salesforce
          # Example: sfdx force:source:deploy -p deploycodeA/content_deploy/ -u YourSalesforceAlias
